# Daily Log — 2026-07-12

## Resumo

Dia de recuperação e consolidação. De manhã houve uma tentativa de redesign
("Raptor 3") que foi guardada em stash após o usuário preferir voltar à última
versão publicada (3 de junho, commit `3c72779`). À tarde, com Fable 5:
resgatamos os fixes de sync que haviam se perdido no revert, extraímos do
stash só a parte boa de sincronização, redesenhamos a abertura do app
(splash nativa + splash Flutter + ícone), auditamos a conformidade de sync em
todos os módulos e geramos archive iOS para nova versão.

## Decisões

- Trabalho "Raptor 3" (redesign visual + sync) preservado em `stash@{0}`
  ("Raptor 3 redesign + melhoria sincronizacao financeira") para revisão futura.
- Estratégia de sync mantida: fila de sync é a única fonte de escrita remota;
  UI confirma na escrita local; `requestSync()` consolida em background.
- Ícone do app: tile teal com "i" vazado (símbolo real da marca) sobre branco.
- Splash: fundo branco com logo em todas as plataformas; Flutter splash com
  logo transparente, gradiente sutil e onda arco-íris como progresso.

## Descobertas importantes

- O commit `ac6ddd1` (await nos recálculos de saldo, 04/06) tinha se PERDIDO
  no revert pré-dark-mode — os 6 `unawaited` bugados estavam de volta.
- O ícone do app ainda era o logo padrão do Flutter.
- `pagamento_fatura_service` NÃO é código morto (usado por gestao_cartoes_mobile).
- `pagar_fatura_page.dart` e `fatura_detalhada_page.dart` não são importados
  por ninguém — são os donos dos 48 erros pré-existentes de flutter analyze.
- `widget_test.dart` ("iPoupei app loads") já falhava no commit de 3 de junho.

## Commits (em ordem)

1. `b383512` fix(transacoes): aguardar recálculo de saldo (cherry-pick do perdido ac6ddd1)
2. `45bea5c` fix(sync): guards contra sobrescrita de pendências + requestSync debounced
3. `8ee87d3` feat(splash): splash nativa com logo (Android/iOS/Web)
4. `624a422` fix(splash): remover "Feliz 2026" do loader
5. `bdbeb81` feat(splash): redesign da abertura (logo transparente, onda arco-íris)
6. `f48ab12` feat(icon): ícone real da marca no lugar do logo Flutter
7. `f4be690` fix(sync): transacao_service escreve remoto só pela fila de sync

## Pendências

- Push dos commits (aguardando ok do usuário).
- Upload da nova versão via Xcode Organizer (archive gerado hoje).
- conta_service: 2 writes diretos da flag `conta_principal` (não crítico).
- Código morto a deletar: transacao_service_simple, pagar_fatura_page,
  fatura_detalhada_page (com os 48 erros de analyze).
- Fases 2-3 do plano de sync: conflict resolution por updated_at, UTC,
  centavos inteiros, Supabase Realtime.
- Stash Raptor 3 a revisar com calma.

## Correção pós-feedback (noite)

Usuário notou que o ícone do app no celular mudou sem ter pedido. O ícone
anterior no repo era o logo padrão do Flutter, e o tile teal com "i" foi
criação não aprovada. Corrigido em `25706dd`: ícone agora é o logo REAL do
iPoupei (wordmark + seta, sem slogan) sobre branco, em Android + iOS.
Build number 5→6. Archive regenerado.

**Lição:** mudanças visíveis de marca (ícone, logo) sempre mostrar preview
e confirmar ANTES de aplicar, mesmo quando a interpretação parece óbvia.

## Memória atualizada

- `sync_bugs_path_a1_correction_plan.md` — status do resgate + auditoria de conformidade.
